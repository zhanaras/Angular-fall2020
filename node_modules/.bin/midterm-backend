#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@kbtu2020/angular-midterm/midterm-backend" "$@"
  ret=$?
else 
  node  "$basedir/../@kbtu2020/angular-midterm/midterm-backend" "$@"
  ret=$?
fi
exit $ret
